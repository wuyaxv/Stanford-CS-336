[project]
name = "tinyllm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "wuyaxu", email = "wuyaxu@foxmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "regex>=2026.1.15",
]

[project.scripts]
tinyllm = "tinyllm:main"

[build-system]
requires = ["uv_build>=0.9.24,<0.10.0"]
build-backend = "uv_build"
